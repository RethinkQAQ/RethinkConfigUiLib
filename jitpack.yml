jdk:
  - openjdk25
before_install:
  - chmod +x gradlew
install:
  - ./gradlew clean build assembleUniversalJars publishToMavenLocal -Pmod.version="${VERSION#v}" -Pjitpack.version="${VERSION}" --no-daemon --stacktrace
